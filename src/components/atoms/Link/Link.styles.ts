import { StyleSheet } from 'react-native';

import { colors } from 'theme';

export const styles = StyleSheet.create({
	link: {
		color: colors.link,
		fontSize: 16,
		fontWeight: '500',
	},
	pressed: {
		opacity: 0.8,
	},
});
