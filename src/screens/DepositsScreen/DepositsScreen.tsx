import { Text } from 'react-native';
import { SafeAreaView } from 'react-native-safe-area-context';

export const DepositsScreen = () => {
	return (
		<SafeAreaView>
			<Text>DepositsScreen</Text>
		</SafeAreaView>
	);
};
